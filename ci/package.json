{
  "name": "a11y-miyar-ci",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CI companion for A11y Miyar \u2014 runs the same axe-core rule sets headlessly and fails the build on new violations.",
  "scripts": {
    "a11y": "node a11y-ci.mjs",
    "a11y:baseline": "node a11y-ci.mjs --update-baseline",
    "postinstall": "playwright install chromium",
    "smoke": "node panel-smoke.mjs && node panel-smoke.mjs ar"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.0",
    "playwright": "^1.49.0"
  }
}
